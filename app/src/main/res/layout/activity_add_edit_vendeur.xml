<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:fitsSystemWindows="true">

    <!-- Toolbar blanche, cohérente avec les autres écrans -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbarAddEdit"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@android:color/white"
        app:titleTextColor="#1C1C1E"
        app:navigationIconTint="#1C1C1E"
        app:toolbarNavigationButtonStyle="@style/MyToolbarNavigationButtonStyle"
        app:title="Ajouter / Modifier" />

    <!-- Ligne fine de séparation, même technique que sur les autres écrans -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="#E0E0E0" />

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="24dp">

        <!-- Photo : avatar rond avec fond violet pâle, cohérent avec
             le détail et la liste, pas d'ombre -->
        <androidx.cardview.widget.CardView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_gravity="center"
            app:cardCornerRadius="60dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="#EEEDFE">

            <ImageView
                android:id="@+id/imgPhotoPreview"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop" />
        </androidx.cardview.widget.CardView>

        <!-- Bouton photo : texte seul, discret, pas de bouton plein -->
        <TextView
            android:id="@+id/btnChoosePhoto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="12dp"
            android:text="Changer la photo"
            android:textColor="#004FEE"
            android:textSize="14sp"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackground"
            android:padding="8dp" />

        <!-- Champs texte : fond gris clair plat, sans cadre, cohérent
             avec la barre de recherche de la liste -->
        <EditText
            android:id="@+id/edtNom"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:hint="Nom du vendeur"
            android:textSize="15sp"
            android:textColorHint="#8E8E93"
            android:background="#F2F2F2"
            android:padding="14dp" />

        <EditText
            android:id="@+id/edtDatenais"
            android:focusable="false"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:hint="Date de naissance"
            android:textSize="15sp"
            android:textColorHint="#8E8E93"
            android:background="#F2F2F2"
            android:padding="14dp" />

        <!-- Espace flexible qui pousse le bouton Enregistrer vers le bas -->
        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Seul bouton plein/coloré de l'écran : l'action principale -->
        <Button
            android:id="@+id/btnSave"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Enregistrer"
            android:textColor="@android:color/white"
            android:backgroundTint="#2F2F2F" />

    </LinearLayout>

</LinearLayout>